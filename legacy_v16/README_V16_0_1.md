# Forge Fitness v16.0.1 — Adaptation Layout Fix

Fixes the Next-Week Adaptation / Program Changes layout at narrow widths. Recommendation cards now stack full-width on phones, use normal word wrapping, and keep the action beneath the explanation. Two-column cards are allowed only when the adaptation container itself is at least 760 CSS pixels wide.
