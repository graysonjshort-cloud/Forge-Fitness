{
  "name": "forge-fitness",
  "version": "17.3.0",
  "private": true,
  "description": "Forge Fitness native Android shell",
  "scripts": {
    "sync:web": "node scripts/sync-web.mjs",
    "cap:sync": "npm run verify:api && npm run sync:web && npx cap sync android",
    "android": "npm run cap:sync && npx cap open android",
    "build:debug": "npm run cap:sync && cd android && gradlew assembleDebug",
    "build:release": "npm run cap:sync && cd android && gradlew bundleRelease",
    "verify:api": "node scripts/verify-native-api.mjs",
    "configure:api": "node scripts/set-api-base.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/app": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/haptics": "^7.0.0",
    "@capacitor/local-notifications": "^7.0.0",
    "@capacitor/preferences": "^7.0.0",
    "@aparajita/capacitor-secure-storage": "^7.1.6"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
