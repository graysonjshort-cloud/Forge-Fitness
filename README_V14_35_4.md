# Forge Fitness v14.35.4

- Cleans up Core Circuit typography and spacing on small phone screens.
- Separates exercise name, core region, set count, target, and Bodyweight metadata.
- Timed core movements now have a real Start/Pause/Reset timer inside Core Tracking.
- Actual timer duration is saved into the existing core-module tracking system.
- Timed core movements cannot be completed with a zero-second result.
- Authenticated app launches now always open Home.
- Active strength sessions still resume from their saved exercise/set position when the user chooses to continue.
- Fresh PWA cache so installed Android/iOS versions can receive this patch.
