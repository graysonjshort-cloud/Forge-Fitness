# Forge Fitness v14.9.1 — Plan Ready Navigation Fix

Fixed the two completion-screen navigation buttons.

- View Plan now has its own `viewplan` action and opens Plan → Workouts.
- Go to Home now has its own `gohome` action and opens Home.
- Removed the accidental shared `startplan` action from both buttons.
- JavaScript syntax and action-wiring regression checks pass.
